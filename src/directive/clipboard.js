// dom operation
export default {
  bind() {
    // initialization
  },
  inserted(el, binding, vnode, oldVnode) {
    // when element is inserted into parent node
    el.focus();
  },
  update() {},
  componentUpdated() {},
  unbined() {}
};
